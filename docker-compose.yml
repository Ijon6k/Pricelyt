version: "3.9"

services:
  postgres:
    image: postgres:16
    container_name: project_postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: project_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  worker1:
    build: ./backend/worker-python
    container_name: project_worker_1
    environment:
      WORKER_ID: "1"
    ports:
      - "8001:8000"
    depends_on:
      - postgres

  worker2:
    build: ./backend/worker-python
    container_name: project_worker_2
    environment:
      WORKER_ID: "2"
    ports:
      - "8002:8000"
    depends_on:
      - postgres

volumes:
  pgdata:
